describe("sum function test", () => {
  it("should be equal 5 when a equal 2 and b equal 3", () => {
    expect(1).toBeTruthy();
  });
  it("should be not equal 5 when a equal 3 and b equal 3", () => {
    expect(0).not.toBeTruthy();
  });
});
